# ROS2_TurtleSim
# my_robot_controller  A ROS2 Python package containing basic robot controller nodes built with `rclpy`.  ## Nodes  ### `draw_circle_node` Makes a TurtleSim turtle drive in a circle by publishing velocity commands to `/turtle1/cmd_vel`.
